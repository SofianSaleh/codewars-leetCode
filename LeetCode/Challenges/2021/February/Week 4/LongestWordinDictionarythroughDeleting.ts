function findLongestWord(s: string, d: string[]): string {
  let ans = '';

  for (const word of d) {
    let i = 0;

    for (const c in s) {
      if (i < word.length && c === word[i]) {
      }
    }
  }
}
