class Solution:
    def constructDistancedSequence(self, n )  :

        # Initialize Variable
        used = set()
        result = [0]*(2*n-1)

        def backtracking(idx):
            if idx > len(result):
                return True
            
        return result
        

Solution().constructDistancedSequence(3)